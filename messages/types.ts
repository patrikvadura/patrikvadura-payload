export interface Dictionary {
  HomePage: {
    title: string
    about: string
  }
  Footer: {
    darkMode: string
    lightMode: string
  }
  NotFound: {
    title: string
    description: string
    backToHome: string
  }
}
